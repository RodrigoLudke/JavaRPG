
package controller;

import model.Personagem;

public class MissaoController {
    public static void missao(String titulo, Personagem p, JogoController jogo) throws InterruptedException {
        // Lógica de missao : Por enquanto, não se faz necessario um controller apenas para a missao
    }
}
